#include <stdio.h>
void main()
{
    int a;
    printf("enter the number");
    scanf("%d",&a);
    if(a%2==0)
    {
        printf("no is even");
    }
    else
    {
        printf("mo is odd");
    }
}